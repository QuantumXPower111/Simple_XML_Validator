# Simple_XML_Validator
A simple console application to validate XML strings based on custom rules: matching opening/closing tags (with exact string content, including attributes), proper nesting, and balance. Implemented in C++ and Python. No XML libraries or regex used, as per specs. 
